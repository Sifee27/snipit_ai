/**
 * Content Processors Index
 * Central export point for all content processors
 */
import { processContent } from './content-processor';

export { processContent };

// Export default object for convenience
export default {
  processContent
};
